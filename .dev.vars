# Cloudflare Workers local dev vars (equivalent of .env for wrangler)
# These mirror the vars set in the Cloudflare dashboard for local development.
# Do NOT commit secrets — add .dev.vars to .gitignore if needed.

MAINTENANCE_MODE=false
MAINTENANCE_SECRET=formo-admin-2026
